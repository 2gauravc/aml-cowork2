
# Shell Company Risk

This is non-runtime explanatory documentation. The active factor definitions,
severities, and actions are in `definition.yaml`; runtime supplies the retained
facts to its configured evaluation path. Editing this Markdown does not change
assessment outcomes.
